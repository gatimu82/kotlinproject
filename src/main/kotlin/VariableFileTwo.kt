fun main(args: Array<String>) {
    var x:Int = 90
    var y:Double =90.0
    println(x + y)

    //calculate interest for a loan borrowed
    var principle = 20000
    var rate = 2
    var time = 5
    var interest = (principle * rate * time) / 100.0
    println(interest)

}